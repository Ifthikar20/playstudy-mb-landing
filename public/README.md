# Static assets

Drop `main-logo.png` (the PlayStudy logo) here. Anything in this folder is
served from the site root at runtime — so this file becomes `/main-logo.png`,
which is what `NavBar.vue` and `Footer.vue` reference.
